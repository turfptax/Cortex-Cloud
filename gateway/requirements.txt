# Runtime deps for Azure App Service (Oryx build). Mirrors pyproject, plus the
# pure-Python SQL Server driver so no system ODBC driver is needed on the host.
fastapi>=0.115
uvicorn[standard]>=0.30
# mcp 2.0.0 (2026-07-28) removed mcp.server.fastmcp, which
# cortex_gateway/mcp_server.py imports. Without the upper bound a
# fresh resolve (CI, or any new image build) lands on 2.x and the
# gateway fails at import. Lift only when ported to the 2.x API.
mcp>=1.2,<2
pydantic>=2.7
sqlalchemy>=2.0
pymssql>=2.3
python-multipart>=0.0.9  # multipart parsing for the cloud Hub chat upload (A2)
